
// always optional, group tests together
// describe();

// this is the test
// it();

const assert = require('assert');

describe('trying out mocha', function() {

  it('runs a test', function() {
    assert.equal(true, true);
  });

});
